{
    "require": {
        "symplify/statie": "^5.3"
    },
    "require-dev": {
        "symplify/latte-to-twig-converter": "dev-master"
    },
    "scripts": {
        "generate": "statie generate source",
        "serve": "php -S 127.0.0.1:80 -t output"
    },
    "prefer-stable": true,
    "minimum-stability": "dev"
}
